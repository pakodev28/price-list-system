server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    # SPA: serve index.html for client-side routes (e.g. /estimates/1).
    location / {
        try_files $uri $uri/ /index.html;
    }
}
